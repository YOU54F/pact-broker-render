#!/bin/bash
echo "Before assembling"

apt-get update -y
apt-get install -y build-essential libsqlite3-dev libpq-dev libyaml-dev

/usr/libexec/s2i/assemble
rc=$?

if [ $rc -eq 0 ]; then
    echo "After successful assembling"
else
    echo "After failed assembling"
fi

exit $rc